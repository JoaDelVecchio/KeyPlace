import { Request, Response } from "express";

export const getPost = (_req: Request, res: Response) => {
  res.json({ msg: "hi" });
};

export const createPost = (_req: Request, res: Response) => {
  res.json({ msg: "hi" });
};

export const updatePost = (_req: Request, res: Response) => {
  res.json({ msg: "hi" });
};

export const deletePost = (_req: Request, res: Response) => {
  res.json({ msg: "hi" });
};
